'use strict';

/* Controllers */

angular.module('crazybride.vendor.controller', [])
    .controller(
        'vendorEditForm',
        [
            '$scope',
            '$cookies',
            'VendorService',
            function($scope, $cookies, VendorService) {

                $scope.validate = function(){


                }
                $scope.save = function(){


                }
            }
        ]
    )
    .controller(
        'vendorList',
        [
            '$scope',
            '$cookies',
            'VendorService',
            function($scope, $cookies, VendorService) {

                $scope.validate = function(){


                }
                $scope.save = function(){


                }
            }
        ]
    )
