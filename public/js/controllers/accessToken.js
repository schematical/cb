'use strict';

/* Controllers */

angular.module('crazybride.accessToken.controller', [])
    .controller(
        'accessTokenEditForm',
        [
            '$scope',
            '$cookies',
            'AccessTokenService',
            function($scope, $cookies, AccessTokenService) {

                $scope.validate = function(){


                }
                $scope.save = function(){


                }
            }
        ]
    )
    .controller(
        'accessTokenList',
        [
            '$scope',
            '$cookies',
            'AccessTokenService',
            function($scope, $cookies, AccessTokenService) {

                $scope.validate = function(){


                }
                $scope.save = function(){


                }
            }
        ]
    )
